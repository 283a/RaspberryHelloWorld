version: '3'
services:
  backend:
    image: ${DOCKER_USERNAME}/your-repo-name:backend-latest
    build: ./backend
    ports:
      - "5000:5000"
  frontend:
    image: ${DOCKER_USERNAME}/your-repo-name:frontend-latest
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend